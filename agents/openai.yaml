interface:
  display_name: "Session Kernel"
  short_description: "Build or audit workflow canon"
  default_prompt: "Use $session-kernel to establish or update a project workflow canon and session checklists."
policy:
  allow_implicit_invocation: false
